#include <stdio.h>
#include <stdlib.io>
#include <time.h>
/**
 * main - prints the stdout
 * Return: 0 if successful
 */
int main(void)
{
	int a;

	for (a = '01'; a <= '10'; a++)
	{
	putchar(a);
	}
	putchar('\n');
	return (0);
}
