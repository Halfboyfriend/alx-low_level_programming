#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints the stdout
 * Return: 0 if successful
 */
int main(void)
{
	int a;

	for (a = 01; a <= 90; a++)
	{
	putchar(a);
	}
	putchar('\n');
	return (0);
}
