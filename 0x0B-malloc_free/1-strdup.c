#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * *_strdup - function
 * @str: char variable
 * Return: Null
 */
char *_strdup(char *str)
{
	char *ptr;
	int a, b;
	int count = 0;

	if (str == NULL)
	{
	return (NULL);
	}
	if (ptr == NULL)
	{
	return (NULL);
	}
}
