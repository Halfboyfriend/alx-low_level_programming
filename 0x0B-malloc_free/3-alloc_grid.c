#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * **alloc_grid - function
 * @width: int var
 * @height: int var
 * Return: NULL
 */
int **alloc_grid(int width, int height)
{
	int a;

	if (width <= 0 || height <= 0)
	{
	return (NULL);
	}
}
